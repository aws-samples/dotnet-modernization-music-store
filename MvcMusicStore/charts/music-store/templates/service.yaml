apiVersion: v1
kind: Service
metadata:
  name: {{ template "music-store.fullname" . }}
  labels:
    app: {{ template "music-store.name" . }}
    chart: {{ template "music-store.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "music-store.name" . }}
    release: {{ .Release.Name }}
